# Private workspace

This workspace contains personal agent configurations.

**Manual population required.** Current workspace actors cannot access the private
Multica workspace. Add agent configurations here by hand for personal projects.

Structure:

```
Private/
  <squad-or-_shared>/
    <agent-slug>/
      agent.json
```
