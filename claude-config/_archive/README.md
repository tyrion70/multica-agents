# claude-config archive (inert)

Historical snapshots from the retired `tyrion70/claude-config` repo (archived
on GitHub, read-only). **Not live config.** Nothing under this directory is
deployed, read, or synced by any host — the live always-on briefs live in
`../chainlayer/CLAUDE.md` and `../private/CLAUDE.md`.

Contents:
- `2026-06-11-pre-reorg/` — pre-Multica laptop memory snapshots + retired
  `CLAUDE.md`/`REPOSITORIES.md` drafts, archived at the `claude-config` repo
  reorg.
- `2026-06-13-profile-memory/` — profile memory folders (chainlayer/private)
  retired when durable learnings were promoted into the `chainlayer-knowledge`
  / `private-knowledge` skills.
- `README.claude-config-repo.md` — the retired repo's own README, kept for
  provenance.

Preserved here as convenience-only history after the archived repo's local
checkouts were removed from `multica-01`/`multica-02`. The upstream repo stays
readable and clonable on GitHub; this copy is redundant with it and exists only
so the local removal loses nothing.
