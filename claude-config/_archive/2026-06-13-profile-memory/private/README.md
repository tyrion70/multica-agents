# Private memory store

Durable, recall-on-relevance notes for **private / homelab** projects/resources,
read by agents on private-profile hosts. Indexed in `MEMORY.md`. To add/change a
memory, follow "Updating this config" in `../CLAUDE.md` (write the file, open a
PR, tell the user).
