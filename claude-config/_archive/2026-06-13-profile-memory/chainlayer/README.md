# ChainLayer memory store

Durable, recall-on-relevance notes for **company** projects/resources, read by
agents on chainlayer-profile hosts. Indexed in `MEMORY.md`. To add/change a
memory, follow "Updating this config" in `../CLAUDE.md` (write the file, open a
PR, tell the user).
